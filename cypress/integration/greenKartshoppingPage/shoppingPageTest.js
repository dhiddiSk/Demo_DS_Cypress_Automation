// / <reference types = "Cypress" />
const {
  Before,
  After,
  Given,
  Then,
  And,
} = require('cypress-cucumber-preprocessor/steps');

Then(/^I go through each product and validate it's presence in order and their corresponding values that includes image, price, category.$/, function () {
   
});
